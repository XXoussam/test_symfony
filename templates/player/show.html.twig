<!DOCTYPE html>
<html>
<head>
    <style>
        table {
            font-family: arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }

        td, th {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #dddddd;
        }
    </style>
</head>
<body>

<h2>HTML Table</h2>

<table>
    <tr>
        <th>name</th>
        <th>username</th>
        <th>stade</th>
        <th>button</th>

    </tr>
   {% for item in response %}
    <tr>
        <td>{{ item.name }}</td>
        <td>{{ item.username }}</td>
        <td>{{ item.stade }}</td>
        <td><a href="{{ path('app_player_delete', {'id': item.id}) }}">delete</a></td>
        <td><a href="{{ path('app_player_update', {'id': item.id}) }}">update</a></td>

    </tr>
    {% endfor %}
</table>
<a href="{{ path('app_player_add') }}" type="submit">add</a>

</body>
</html>

